<Window x:Class="GIS.Windows.AttributeQueryWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:GIS.Windows"
        xmlns:conv="clr-namespace:GIS.Classes.Converters"
        mc:Ignorable="d"
        Title="AttributeQueryWindow" Height="300" Width="400" WindowStartupLocation="CenterScreen">
    <Window.Resources>
        <conv:BoolToVisibilityConverter x:Key="BoolToVisible"/>
    </Window.Resources>

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Выбор слоя -->
        <GroupBox Header="Слой" Grid.Row="0" Margin="0,5">
            <ComboBox ItemsSource="{Binding Layers}" DisplayMemberPath="Name" 
                      SelectedItem="{Binding SelectedLayer}" Margin="5"/>
        </GroupBox>

        <!-- Условие -->
        <GroupBox Header="Условие" Grid.Row="1" Margin="0,5">
            <Grid Margin="5">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <ComboBox Grid.Column="0" ItemsSource="{Binding AvailableAttributes}" 
                          SelectedItem="{Binding SelectedAttribute}" Margin="2"/>
                <ComboBox Grid.Column="1" ItemsSource="{Binding Operators}" 
                          SelectedItem="{Binding SelectedOperator}" Margin="2" Width="70"/>
                <TextBox Grid.Column="2" Text="{Binding ConditionValue, UpdateSourceTrigger=PropertyChanged}" Margin="2"/>
            </Grid>
        </GroupBox>

        <!-- Выбор результата -->
        <GroupBox Header="Результат" Grid.Row="2" Margin="0,5">
            <StackPanel Margin="5">
                <RadioButton Content="Выделить объекты" GroupName="ResultMode" IsChecked="{Binding ResultHighlight}" Margin="0,2"/>
                <RadioButton Content="Создать новый слой" GroupName="ResultMode" IsChecked="{Binding ResultCreateLayer}" Margin="0,2"/>
            </StackPanel>
        </GroupBox>

        <!-- Кнопки -->
        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0">
            <Button Content="Выполнить" Command="{Binding ExecuteCommand}" Width="80" Margin="5" IsDefault="True"/>
            <Button Content="Отмена" Command="{Binding CancelCommand}" Width="80" Margin="5" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>
