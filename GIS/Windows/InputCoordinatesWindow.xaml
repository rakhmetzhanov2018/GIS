<Window x:Class="GIS.Windows.InputCoordinatesWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:GIS.Windows"
        mc:Ignorable="d"
        Title="Окно ввода координат" MaxHeight="400" Height="Auto" Width="300" 
        WindowStartupLocation="CenterScreen" Loaded="Window_Loaded">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Row="0" Grid.Column="0" Text="Долгота:" Margin="5"/>
        <TextBox x:Name="LonBox" Grid.Row="0" Grid.Column="1" Margin="5"/>

        <TextBlock Grid.Row="1" Grid.Column="0" Text="Широта:" Margin="5"/>
        <TextBox x:Name="LatBox" Grid.Row="1" Grid.Column="1" Margin="5"/>

        <Button Grid.Row="2" Grid.Column="0" Content="OK" Width="60" Margin="5" Click="Ok_Click"/>
        <Button Grid.Row="2" Grid.Column="1" Content="Отмена" Width="60" Margin="5" Click="Cancel_Click" HorizontalAlignment="Right"/>
    </Grid>
</Window>
