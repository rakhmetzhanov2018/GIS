<Window x:Class="GIS.Windows.CreateNewLayerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:GIS.Windows"
        xmlns:controls="clr-namespace:GIS.UserControls"
        mc:Ignorable="d"
        Title="Создание нового слоя" Height="Auto" Width="500" UseLayoutRounding="True" SnapsToDevicePixels="True" WindowStyle="ThreeDBorderWindow" ResizeMode="NoResize" WindowStartupLocation="CenterScreen">

    <Grid Margin="20">
        <Grid.Resources>
            <Style TargetType="TextBlock">
                <Setter Property="FontFamily" Value="Arial"/>
                <Setter Property="FontSize" Value="14"/>
                <Setter Property="Margin" Value="5"/>
                <Setter Property="VerticalAlignment" Value="Center"/>
            </Style>

            <Style TargetType="TextBox">
                <Setter Property="Margin" Value="5"/>
                <Setter Property="FontSize" Value="14"/>
                <Setter Property="VerticalAlignment" Value="Center"/>
            </Style>
        </Grid.Resources>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*" MinHeight="40"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>


        <TextBlock Grid.Row="0" Grid.Column="0" Text="Название слоя:"/>
        <TextBox x:Name="LayerNameTextBox" Text="{Binding LayerName, UpdateSourceTrigger=PropertyChanged}" 
                 Grid.Row="0" Grid.Column="1"/>

        <TextBlock Grid.Row="1" Grid.Column="0" Text="Тип геометрии:"/>
        <ComboBox x:Name="GeometryTypeComboBox" ItemsSource="{Binding GTypes}" SelectedValue="{Binding GeoType, UpdateSourceTrigger=PropertyChanged}"
                  Grid.Row="1" Grid.Column="1" SelectedIndex="0" Margin="5" HorizontalAlignment="Left" MinWidth="100">
        </ComboBox>

        <controls:FeatureAttributesCreationUserControl 
            Grid.Row="2" Grid.ColumnSpan="2"
            ViewModel="{Binding}" Margin="0,10,0,0"/>

        <Button x:Name="CreateLayer_CancelButton" 
                Command="{Binding CancelCommand}"
                Grid.Row="3" Grid.Column="0"
                Content="Отменить" 
                MinWidth="100" 
                VerticalAlignment="Bottom" 
                HorizontalAlignment="Left"
                Margin="5,20,0,0"/>
        
        <Button x:Name="CreateLayer_ApplyButton" 
                Command="{Binding ApplyCommand}"
                Grid.Row="3" Grid.Column="1"
                Content="Применить" MinWidth="100" 
                VerticalAlignment="Bottom" HorizontalAlignment="Right" 
                Margin="0,20,5,0"/>
    </Grid>
</Window>
