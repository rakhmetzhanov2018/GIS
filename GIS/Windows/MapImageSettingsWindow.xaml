<Window x:Class="GIS.Windows.MapImageSettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:GIS.Windows"
        mc:Ignorable="d"
        Title="MapImageSettingsWindow" Height="220" Width="400" WindowStartupLocation="CenterScreen">
    <Grid>
        <Grid Margin="10">
            <Grid.Resources>
                <Style TargetType="TextBlock">
                    <Setter Property="FontFamily" Value="Arial"/>
                    <Setter Property="FontSize" Value="14"/>
                    <Setter Property="Margin" Value="5"/>
                    <Setter Property="VerticalAlignment" Value="Center"/>

                </Style>

                <Style TargetType="TextBox">
                    <Setter Property="Margin" Value="5"/>
                    <Setter Property="FontSize" Value="14"/>
                    <Setter Property="VerticalAlignment" Value="Center"/>
                </Style>
            </Grid.Resources>

            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="50"/>
            </Grid.RowDefinitions>

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Row="0" Grid.Column="0" Text="MinLon:"/>
            <TextBox x:Name="MinLonTextBox" Grid.Row="0" Grid.Column="1"/>

            <TextBlock Grid.Row="1" Grid.Column="0" Text="MaxLon:"/>
            <TextBox x:Name="MaxLonTextBox" Grid.Row="1" Grid.Column="1"/>

            <TextBlock Grid.Row="2" Grid.Column="0" Text="MinLat:"/>
            <TextBox x:Name="MinLatTextBox" Grid.Row="2" Grid.Column="1"/>

            <TextBlock Grid.Row="3" Grid.Column="0" Text="MaxLat:"/>
            <TextBox x:Name="MaxLatTextBox" Grid.Row="3" Grid.Column="1"/>

            <Button x:Name="LayerSettingsCancelButton" Grid.Row="4" Grid.Column="0" Content="Отменить" MinWidth="100" VerticalAlignment="Bottom" HorizontalAlignment="Left" Click="LayerSettingsCancelButton_Click"/>
            <Button x:Name="LayerSettingsApplyButton" Grid.Row="4" Grid.Column="1" Content="Применить" MinWidth="100" VerticalAlignment="Bottom" HorizontalAlignment="Right" Click="LayerSettingsApplyButton_Click"/>
        </Grid>
    </Grid>
</Window>
